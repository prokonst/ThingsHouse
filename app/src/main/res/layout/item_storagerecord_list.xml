<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <data>
        <variable
            name="storageRecord"
            type="com.prokonst.thingshouse.model.dataview.StorageRecord" />
    </data>
    
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="3dp"
        app:cardCornerRadius="4dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp">

            <TextView
                android:id="@+id/nameTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:hint="ThingName"
                android:text="@{storageRecord.name}"
                android:textSize="18sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <ImageView
                android:id="@+id/mainPhotoImageView"
                android:layout_width="150dp"
                android:layout_height="150dp"
                android:src="@{storageRecord.mainPhotoSrc}"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/nameTextView" />

            <TextView
                android:id="@+id/barCodeTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:hint="BarCode"
                android:text="@{storageRecord.barCode}"
                android:textSize="10sp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/mainPhotoImageView" />

            <TextView
                android:id="@+id/quantityTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:hint="Quantity"
                android:text="@{Double.toString(storageRecord.quantity)}"
                android:textSize="16sp"
                android:layout_marginLeft="8dp"
                app:layout_constraintBottom_toBottomOf="@+id/mainPhotoImageView"
                app:layout_constraintStart_toEndOf="@+id/mainPhotoImageView"
                app:layout_constraintTop_toTopOf="@+id/mainPhotoImageView" />

            <TextView
                android:id="@+id/unitTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:hint="Unit"
                android:text="@{storageRecord.unit}"
                android:textSize="16sp"
                android:layout_marginLeft="8dp"
                app:layout_constraintBottom_toBottomOf="@+id/quantityTextView"
                app:layout_constraintStart_toEndOf="@+id/quantityTextView"
                app:layout_constraintTop_toTopOf="@+id/quantityTextView" />

        </androidx.constraintlayout.widget.ConstraintLayout>
        <!--
        <LinearLayout
            android:orientation="vertical"
            android:padding="8dp"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:id="@+id/nameTextView"
                android:text="@{storageRecord.name}"
                android:textSize="18sp"
                android:textStyle="bold"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
            <TextView
                android:id="@+id/unitTextView"
                android:text="@{storageRecord.unit}"
                android:textSize="10sp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
            <ImageView
                android:id="@+id/mainPhotoImageView"
                android:layout_width="150dp"
                android:layout_height="150dp"
                android:src="@{storageRecord.mainPhotoSrc}"
                />
            <TextView
                android:id="@+id/barCodeTextView"
                android:text="@{storageRecord.barCode}"
                android:textSize="10sp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

        </LinearLayout>-->

    </androidx.cardview.widget.CardView>

</layout>
